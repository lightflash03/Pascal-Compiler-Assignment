program NumberOfDigits;
var
  number, fPra: Integer;
  count, checkFloat: Real;
  pressF: Char;
  CC_killed: boolean;
  numbers: array[3..10] of Real;
begin
  number := 1;
  fPra := 3;
  count := 4.5;
  numbers[5] := 10.0;
  write(number);
  write("Bitch");
  write(number, fPra, count);
  read(count);
  write(count);
  write(numbers[5]);
end.
